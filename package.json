{
  "name": "nodejsproject-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix Backeend run dev\" \"npm --prefix Frontend run dev\"",
    "build": "npm --prefix Frontend install && npm --prefix Frontend run build && npm --prefix Backeend install",
    "start": "npm --prefix Backeend start"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
